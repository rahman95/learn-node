extends ../layout

block content
  .inner
    h2 #{title}
      .map(style="margin-top: 2rem;")
        .autocomplete
          input.autocomplete__input(type="text" placeholder="Search", name="geolocate")

        #map
          p Loading Map...